---
title: 链表实现多项式及相关操作
date: 2017-03-19 18:15:06
categories: 技术
tags: 算法
---

多项式数据结构：
``` c
typedef struct polyNode *polyNodePtr;
struct polyNode {
    int coef; // 整型系数部分
    int expon; // 指数部分
    poluNodePtr next;
};
```


多项式加法操作：
``` c
polyNodePtr polyAdd(polyNodePtr a, polyNodePtr b)
{
    polyNodePtr c, tail, zeroNode;
    tail = malloc(sizeof(tail));
    c = tail;

    while (a && b)
    {
        if (a->expon == b->expon)
        {
            sum = a->coef + b->coef;
            if (sum)
                attach(sum, a->expon, &tail);

            a = a->next;
            b = b->next;
        }
        else if (a->expon > b->expon)
        {
            attach(a->coef, a->expon, &tail);
            
            a = a->next;
        }
        else
        {
            attach(b->coef, b->expon, &tail);

            b = b->next;
        }
    }

    for (; a; a = a->next) attach(a->coef, a->expon, &tail);
    for (; b; b = b->next) attach(b->coef, b->expon, &tail);

    tail->next = NULL;

    zeroNode = c;
    c = c->next;
    free(zeroNode);

    return c;
}
```

多项式销毁：
``` c
void erase(polyNodePtr *p)
{
    polyNodePtr node;
    while (*p)
    {
        node = *p;
        *p = (*p)->next;
        free(node);
    }
}
```

代码：[poly\_list.c](https://github.com/YoungZiyi/COMPUTER_FUNDAMENTALS/blob/master/poly_list.c)
